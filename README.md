# manage-finances

ระบบทดสอบ: “การเบิกถอนเงิน & รายงานสถานะการถอน” (Single Page App)
ส่งเป็น: GitHub repo หรือ ZIP (ไฟล์นี้คือ README สำหรับการส่ง)

---
## สิ่งที่รวมในโปรเจค
- Frontend: React + Vite + TypeScript
- โครงสร้างหลัก:
  - `src/` : โค้ดต้นทาง (รวม `App.tsx`, `main.tsx`, `api/mockApi.ts`)
## ฟีเจอร์ (ตามโจทย์)
- หน้ารายการคำขอเบิกถอน (list) พร้อมสถานะ
- ฟอร์มสร้างคำขอเบิกถอนใหม่ (รองรับอัพโหลดรูป/วิดีโอ หลายไฟล์)
- หน้ารายละเอียดแต่ละรายการ (แสดง history + attachments)
- ค้นหา / กรอง ตามสถานะ, ชื่อ, TX ID
- ใช้ mock API (`src/api/mockApi.ts`) แทน backend จริง

## ติดตั้งและรัน (Development)
1. ติดตั้ง dependencies:

```powershell
2. รันโหมดพัฒนา:

```powershell
3. เปิดเบราว์เซอร์ที่:

```
หมายเหตุ: โปรเจคอาจใช้พอร์ตอื่น (Vite เลือกพอร์ตที่ว่าง) — ดูเทอร์มินัลเมื่อรัน

## Build (สำหรับส่งไฟล์สำเร็จ)
```powershell
npm run build
```
## ตัวอย่างภาพหน้าจอ
- (วางภาพตัวอย่างใน `screenshots/` หรือ `docs/` แล้วอ้างใน README)

## ข้อจำกัด / หมายเหตุ
- ไม่มี backend จริง — ข้อมูลมาจาก `mockApi` ใน `src/api/mockApi.ts` (สามารถเปลี่ยนเป็น API จริงได้ง่าย)
- ไฟล์ `public/favicon.ico` เพิ่มเป็น placeholder เพื่อป้องกันการแสดงไอคอนเดิมจากแคช

## วิธีการส่ง
- GitHub (แนะนำ): สร้าง repo แล้ว push ตามคำสั่งที่ให้ไว้ใน README
- ZIP: สร้างไฟล์ zip ของโปรเจคโดยไม่รวม `node_modules/` (ตัวอย่างคำสั่งใน README)

## Contact (สำหรับส่งงาน)
- ชื่อ: Ratchapon Yamklai
- โทร: 065-726-6900
- อีเมล: Ratchapon@urbanwaygroup.com

---
ถ้าต้องการผมจะ:
- สร้างไฟล์ ZIP ของโปรเจคใน workspace (`manage-finances-submission.zip`) ที่ไม่รวม `node_modules` และ `.git` (ผมจะทำให้ถ้าต้องการ), หรือ
- ให้คำสั่งทีละบรรทัดเพื่อ push โค้ดขึ้น GitHub: ผมไม่สามารถ push ให้บนบัญชี GitHub ของคุณโดยตรง แต่จะให้คำสั่งที่ชัดเจนให้คุณรันได้

บอกผมว่าต้องการให้ผม `create-zip` หรือแค่ให้คำสั่ง push ขึ้น GitHub เท่านั้น
# การเบิกถอนเงิน 

หัวข้อ: ระบบ “การเบิกถอนเงิน & รายงานสถานะการถอน”

กำหนดส่ง: ภายในวันพุธ เวลา 18:00
ส่งเป็น: GitHub repo หรือ zip file พร้อม README รายละเอียดตามไฟล์แนบ

โจทย์สรุป:
- ทำ Single Page App (React/Next.js, แนะนำ TypeScript) ที่มี:
  1) หน้ารายการคำขอเบิกถอน (list) พร้อมสถานะ
  2) ฟอร์มส่งคำขอเบิกถอนใหม่ (support upload รูป/วิดีโอ)
  3) หน้ารายละเอียดแต่ละรายการ (แสดง history + attachments)
  4) filter / search by status/name/tx id



** วิธีรัน ** 
1. ติดตั้ง npm install
2. npm run dev
3. เปิดเบราว์เซอร์ http://localhost:3000



** front-end การเบิกถอนเงิน ** (React + Vite + TypeScript)

ไฟล์ App.tsx เป็นไฟล์การจัดการข้อมูลต่างๆ เช่น การสร้างฟอร์มคำขอ , รายการ , การค้นหา 
ไฟล์ App.css เป็นสไตล์ css ทั้งหมด
ไฟล์ mockApi.ts เป็นไฟล์ mock API 
โฟลเดอร์ public เป็นรูปไอคอนและรูปภาพที่อัปโหลดไว้เพื่อมาใช้ใน Mock Data 

ในหน้า UI 
- แสดงช่อง Input การค้นหาชื่อ/TX ID/รายการ 
- แสดง filter สถานะ ได้แก่ รออนุมัติ/อนุมัติ/ปฏิเสธ
- ตารางรายการเบิกถอน แสดงข้อมูล TX ID/ชื่อ/รายละเอียด/จำนวน/สถานะ/วันที่ คลิกเข้าไปที่รายการเบิกถอนที่ต้องการเพื่อดูรายละเอียดเพิ่มเติม TX ID/ชื่อ/รายละเอียด/จำนวน/สถานะ/วันที่/รูปภาพหรือวิดีโอที่อัปโหลด 
- ฟอร์มคำขอเบิกถอนเงินที่ + แสดง Popup ให้กรอกข้อมูล ชื่อผู้ขอ/รายละเอียดเหตุผลการเบิกถอน/จำนวน (บาท) และอัปโหลดรูปภาพหรือวิดีโอรองรับหลายไฟล์


